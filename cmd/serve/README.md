# `serve` Command

The serve command is the command that starts the service. This is the main entry point for the server itself.

## Options

- `-c, --config` specify the config file location, also settable via env var

