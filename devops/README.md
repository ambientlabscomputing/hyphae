# Hyphea DevOps

## Structure

**`orb_scripts`**: scripts for managing local OrbStack VMs, loading the DevOps payload and triggering its deployment

**`payload`**: a seed that contains all scripts necessary to autodeploy a full Hyphae Cloud Service. It assumes to run in a blank slate Ubuntu server.
